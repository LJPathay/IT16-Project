@model ljp_itsolutions.Services.AdminDashboardData
@{
    ViewData["Title"] = "Admin Dashboard";
    Layout = "_Layout";
}

<div class="container-fluid px-4 py-3">

    <!-- Risk Alert Banner -->
    @if (Model.RiskAlerts != null && Model.RiskAlerts.Any())
    {
        <div class="row mb-4">
            <div class="col-12">
                <div class="alert alert-danger border-0 shadow-sm rounded-3 py-3 px-4 d-flex align-items-center mb-0">
                    <div class="stats-icon bg-danger text-white rounded-circle me-3" style="width: 40px; height: 40px; display: flex; align-items: center; justify-content: center;">
                        <i class="fas fa-exclamation-triangle"></i>
                    </div>
                    <div>
                        <h6 class="fw-bold mb-1">System Intelligence: Risk Exposure Detected</h6>
                        @foreach (var alert in Model.RiskAlerts)
                        {
                            <p class="mb-0 small text-danger">@alert</p>
                        }
                    </div>
                </div>
            </div>
        </div>
    }

    <!-- KPI Strip -->
    <div class="row g-3 mb-4">
        <!-- Portfolio Card -->
        <div class="col-md-3">
            <div class="kpi-card-premium brand-primary">
                <div class="kpi-content">
                    <p class="kpi-title">Product Portfolio</p>
                    <span class="kpi-main-val">@Model.PortfolioValue</span>
                    <div class="kpi-trend text-muted">
                        Active Catalog Items
                    </div>
                </div>
                <div class="kpi-icon-box">
                    <i class="fas fa-boxes"></i>
                </div>
            </div>
        </div>

        <!-- Throughput Card -->
        <div class="col-md-3">
            <div class="kpi-card-premium brand-success">
                <div class="kpi-content">
                    <p class="kpi-title">Daily Throughput</p>
                    <span class="kpi-main-val">@Model.DailyThroughput</span>
                    <div class="kpi-trend @(Model.ThroughputGrowth >= 0 ? "text-success" : "text-danger")">
                        <i class="fas @(Model.ThroughputGrowth >= 0 ? "fa-arrow-up" : "fa-arrow-down")"></i>
                        @Math.Abs(Model.ThroughputGrowth).ToString("N1")% Flow Velocity
                    </div>
                </div>
                <div class="kpi-icon-box">
                    <i class="fas fa-bolt"></i>
                </div>
            </div>
        </div>

        <!-- Revenue Card -->
        <div class="col-md-6">
            <div class="kpi-card-premium brand-indigo">
                <div class="kpi-content">
                    <p class="kpi-title">Gross Operating Revenue</p>
                    <span class="kpi-main-val">₱@Model.TotalGrossRevenue.ToString("N2")</span>
                    <div class="kpi-trend text-muted">
                        System-wide Consolidated Revenue
                    </div>
                </div>
                <div class="kpi-icon-box">
                    <i class="fas fa-chart-line"></i>
                </div>
            </div>
        </div>
    </div>



    <!-- Main Analytics Row -->
    <div class="row g-4 mb-4">
        <!-- Sales Trend Column -->
        <div class="col-lg-8">
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden h-100">
                <div class="card-header bg-white py-3 px-4 d-flex justify-content-between align-items-center">
                    <h6 class="fw-bold mb-0">Sales & Throughput Velocity</h6>
                    <button class="btn btn-link btn-sm text-decoration-none p-0 text-muted">Details <i class="fas fa-chevron-right ms-1 small"></i></button>
                </div>
                <div class="card-body p-4">
                    <div style="height: 300px;">
                        <canvas id="salesTrendsChart"></canvas>
                    </div>
                </div>
            </div>
        </div>

        <!-- Composition Column -->
        <div class="col-lg-4">
            <div class="card border-0 shadow-sm rounded-4 overflow-hidden h-100">
                <div class="card-header bg-white py-3 px-4">
                    <h6 class="fw-bold mb-0">Identity Distribution</h6>
                </div>
                <div class="card-body p-4">
                    <div style="height: 300px;" class="d-flex flex-column align-items-center justify-content-center">
                        <canvas id="userRolesChart"></canvas>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Recent Transitions & Logs -->
    <div class="row g-4">
        <div class="col-12">
            <div class="card border-0 shadow-sm rounded-4">
                <div class="card-header bg-white py-3 px-4 d-flex justify-content-between align-items-center">
                    <h6 class="fw-bold mb-0">Executive Audit Trail</h6>
                    <a href="@Url.Action("AuditLogs")" class="btn btn-link btn-sm text-decoration-none p-0">Detailed Logs <i class="fas fa-external-link-alt ms-1 x-small"></i></a>
                </div>
                <div class="card-body p-0">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle mb-0">
                            <thead class="bg-slate-50">
                                <tr>
                                    <th class="ps-4 border-0 x-small text-uppercase text-muted fw-bold">Operator</th>
                                    <th class="border-0 x-small text-uppercase text-muted fw-bold">Classification</th>
                                    <th class="border-0 x-small text-uppercase text-muted fw-bold">Action Event</th>
                                    <th class="pe-4 border-0 x-small text-uppercase text-muted fw-bold text-end">Execution Time</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var log in Model.RecentActivityLogs)
                                {
                                    <tr>
                                        <td class="ps-4">
                                            <div class="d-flex align-items-center gap-2">
                                                <div class="avatar-sm rounded-circle bg-slate-100 d-flex align-items-center justify-content-center text-slate-600 fw-bold border">
                                                    @(log.User?.FullName?[0] ?? 'S')
                                                </div>
                                                <div>
                                                    <p class="mb-0 small fw-bold">@(log.User?.FullName ?? "System Core")</p>
                                                    <p class="xx-small text-muted mb-0">ID: #@log.AuditID</p>
                                                </div>
                                            </div>
                                        </td>
                                        <td>
                                            @{
                                                var actionLower = log.Action.ToLower();
                                                var pillClass = "bg-slate-50 text-slate-600 border-slate-100";
                                                var category = "Audit";
                                                
                                                if (actionLower.Contains("create") || actionLower.Contains("added") || actionLower.Contains("enrolled")) {
                                                    pillClass = "bg-success bg-opacity-10 text-success border-success border-opacity-10";
                                                    category = "Create";
                                                } else if (actionLower.Contains("edit") || actionLower.Contains("update") || actionLower.Contains("modify")) {
                                                    pillClass = "bg-primary bg-opacity-10 text-primary border-primary border-opacity-10";
                                                    category = "Update";
                                                } else if (actionLower.Contains("delete") || actionLower.Contains("remove") || actionLower.Contains("archive") || actionLower.Contains("void")) {
                                                    pillClass = "bg-danger bg-opacity-10 text-danger border-danger border-opacity-10";
                                                    category = "Delete";
                                                } else if (actionLower.Contains("login") || actionLower.Contains("authenticated")) {
                                                    pillClass = "bg-purple bg-opacity-10 text-purple border-purple border-opacity-10";
                                                    category = "Login";
                                                }
                                            }
                                            <span class="badge rounded-pill @pillClass px-3" style="@(category == "Login" ? "background-color: #a855f7; color: white;" : "") @(category == "Audit" ? "background-color: #f1f5f9; color: #64748b;" : "")">@category</span>
                                        </td>
                                        <td><span class="small text-slate-700">@log.Action</span></td>
                                        <td class="pe-4 text-end">
                                            <span class="text-muted xx-small">@log.Timestamp.ToString("MMM dd • HH:mm")</span>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    :root {
        --primary-blue: #3b82f6;
        --secondary-slate: #64748b;
        --border-color: #e2e8f0;
        --bg-slate-50: #f8fafc;
    }

    .kpi-card { transition: transform 0.2s ease, box-shadow 0.2s ease; cursor: pointer; }
    .kpi-card:hover { transform: translateY(-5px); box-shadow: 0 10px 15px -3px rgba(0,0,0,0.1) !important; }
    .kpi-icon-bg { position: absolute; right: -10px; bottom: -10px; font-size: 5rem; color: rgba(0,0,0,0.03); transform: rotate(-15deg); pointer-events: none; }
    .kpi-badge { background: rgba(59, 130, 246, 0.1); color: var(--primary-blue); width: 32px; height: 32px; border-radius: 8px; display: flex; align-items: center; justify-content: center; font-size: 0.8rem; }
    
    .live-pulse { width: 8px; height: 8px; background: #10b981; border-radius: 50%; box-shadow: 0 0 0 rgba(16, 185, 129, 0.4); animation: pulse 2s infinite; }
    @@keyframes pulse { 0% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0.4); } 70% { box-shadow: 0 0 0 10px rgba(16, 185, 129, 0); } 100% { box-shadow: 0 0 0 0 rgba(16, 185, 129, 0); } }

    .btn-white { background: white; border: 1px solid var(--border-color); color: var(--secondary-slate); font-weight: 500; }
    .btn-white.active { background: #f1f5f9; color: var(--primary-blue); border-color: var(--primary-blue); }

    .bg-slate-50 { background-color: var(--bg-slate-50); }
    .text-slate-400 { color: #94a3b8; }
    .text-slate-700 { color: #334155; }
    .bg-blue-500 { background-color: #3b82f6; }
    .bg-purple { background-color: #a855f7; }
    .text-purple { color: #a855f7; }
    .border-purple { border-color: #a855f7; }
    .avatar-sm { width: 32px; height: 32px; font-size: 0.75rem; }
    .xx-small { font-size: 0.65rem; }
</style>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/chart.js"
    integrity="sha384-52b3468745678901234567890123456789012345678901234567890123456789" 
    crossorigin="anonymous"></script>
    <script>
        const chartColors = {
            blue: '#3b82f6',
            indigo: '#6366f1',
            emerald: '#10b981',
            amber: '#f59e0b',
            rose: '#f43f5e',
            slate: '#94a3b8'
        };

        // Sales Trends Chart
        const ctxSales = document.getElementById('salesTrendsChart');
        if (ctxSales) {
            const gradient = ctxSales.getContext('2d').createLinearGradient(0, 0, 0, 400);
            gradient.addColorStop(0, 'rgba(59, 130, 246, 0.1)');
            gradient.addColorStop(1, 'rgba(59, 130, 246, 0)');

            new Chart(ctxSales, {
                type: 'line',
                data: {
                    labels: [@Html.Raw(string.Join(", ", Model.SalesTrendLabels.Select(l => $"'{l}'")))],
                    datasets: [{
                        label: 'Throughput Value',
                        data: [@string.Join(", ", Model.SalesTrendData)],
                        borderColor: chartColors.blue,
                        backgroundColor: gradient,
                        borderWidth: 3,
                        tension: 0.4,
                        fill: true,
                        pointRadius: 0,
                        pointHoverRadius: 6,
                        pointHoverBackgroundColor: '#fff',
                        pointHoverBorderWidth: 3
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { display: false },
                        tooltip: {
                            mode: 'index',
                            intersect: false,
                            padding: 12,
                            backgroundColor: '#0f172a',
                            titleFont: { size: 14 },
                            bodyFont: { size: 13 },
                            callbacks: {
                                label: (ctx) => ` ₱${ctx.parsed.y.toLocaleString()}`
                            }
                        }
                    },
                    scales: {
                        x: { grid: { display: false } },
                        y: { 
                            beginAtZero: true,
                            grid: { color: '#f1f5f9' },
                            ticks: { callback: (val) => '₱' + val.toLocaleString() }
                        }
                    }
                }
            });
        }

        // Roles Composition
        const ctxRoles = document.getElementById('userRolesChart');
        if (ctxRoles) {
            new Chart(ctxRoles, {
                type: 'doughnut',
                data: {
                    labels: @Html.Raw(Json.Serialize(Model.RoleDistribution.Select(r => r.Role))),
                    datasets: [{
                        data: @Html.Raw(Json.Serialize(Model.RoleDistribution.Select(r => r.Count))),
                        backgroundColor: [chartColors.blue, chartColors.indigo, chartColors.emerald, chartColors.rose, chartColors.amber, chartColors.slate],
                        borderWidth: 0,
                        cutout: '70%',
                        hoverOffset: 12
                    }]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        legend: { 
                            display: true,
                            position: 'bottom',
                            labels: {
                                usePointStyle: true,
                                padding: 20,
                                font: { size: 11, weight: 'bold' },
                                generateLabels: function(chart) {
                                    const data = chart.data;
                                    if (data.labels.length && data.datasets.length) {
                                        const dataset = data.datasets[0];
                                        const total = dataset.data.reduce((a, b) => a + b, 0);
                                        return data.labels.map((label, i) => {
                                            const value = dataset.data[i];
                                            const percentage = total > 0 ? ((value / total) * 100).toFixed(1) : 0;
                                            return {
                                                text: `${label} (${percentage}%)`,
                                                fillStyle: dataset.backgroundColor[i],
                                                strokeStyle: dataset.backgroundColor[i],
                                                lineWidth: 0,
                                                index: i
                                            };
                                        });
                                    }
                                    return [];
                                }
                            }
                        },
                        tooltip: {
                            padding: 12,
                            backgroundColor: '#0f172a',
                            titleFont: { size: 14 },
                            bodyFont: { size: 13 },
                            callbacks: {
                                label: (ctx) => {
                                    const total = ctx.dataset.data.reduce((a, b) => a + b, 0);
                                    const value = ctx.parsed;
                                    const percentage = ((value / total) * 100).toFixed(1);
                                    return ` ${ctx.label}: ${value} Users (${percentage}%)`;
                                }
                            }
                        },
                        datalabels: { display: false }
                    }
                }
            });
        }
    </script>
}
