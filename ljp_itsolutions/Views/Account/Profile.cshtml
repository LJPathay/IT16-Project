@model ljp_itsolutions.Models.User
@{
    ViewData["Title"] = "Account Profile & Settings";
    // Get initials for the avatar if no picture is set
    var initials = string.Join("", Model.FullName.Split(' ').Select(n => n[0])).ToUpper();
}

<div class="container-fluid py-4 ux-animate-fade-in">
    <div class="row g-4">
        <!-- Profile Header - Premium Espresso Theme -->
        <div class="col-12">
            <div class="card border-0 shadow-sm rounded-lg overflow-hidden mb-4 profile-header-premium">
                <div class="card-body p-5 text-white position-relative">
                    <div class="d-flex align-items-center gap-4 flex-wrap flex-md-nowrap">
                        <div class="profile-picture-container shadow-lg">
                            @if (!string.IsNullOrEmpty(Model.ProfilePictureUrl))
                            {
                                <img src="@Model.ProfilePictureUrl" alt="@Model.FullName" class="profile-pic" />
                            }
                            else
                            {
                                <div class="profile-initials">@initials</div>
                            }
                        <button type="button" class="edit-overlay" onclick="document.getElementById('profilePictureFile').click()" aria-label="Change profile picture" style="border:none; background:none; padding:0;">
                            <i class="fas fa-camera"></i>
                        </button>
                        </div>
                        <div class="profile-meta">
                            <h1 class="ux-title text-white mb-1">@Model.FullName</h1>
                            <div class="d-flex align-items-center gap-2 opacity-75">
                                <span class="badge bg-amber text-white px-3">@Model.Role</span>
                                <span class="text-white-50">•</span>
                                <span class="text-white-50">@@@Model.Username</span>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Personal Information -->
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm rounded-lg overflow-hidden h-100 ux-card">
                <div class="card-header bg-white py-3 border-bottom">
                    <h6 class="fw-bold mb-0 text-dark">Personal Information</h6>
                </div>
                <div class="card-body p-4">
                    <form asp-action="UpdateProfile" method="post" enctype="multipart/form-data">
                        <div class="mb-3">
                            <label for="fullNameInput" class="modern-form-label">Full Name</label>
                            <input type="text" id="fullNameInput" name="fullName" class="modern-form-control w-100" value="@Model.FullName" required aria-label="fullName">
                        </div>
                        <div class="mb-3">
                            <label for="emailInput" class="modern-form-label">Email Address</label>
                            <input type="email" id="emailInput" name="email" class="modern-form-control w-100" value="@Model.Email" placeholder="user@coffee.local" aria-label="email">
                        </div>
                        <div class="mb-4">
                            <label for="profilePictureUrl" class="modern-form-label">Profile Image</label>
                            <div class="input-group">
                                <span class="input-group-text bg-white border-2 border-end-0"><i class="fas fa-link text-muted"></i></span>
                                <input id="profilePictureUrl" type="text" name="profilePictureUrl" class="modern-form-control flex-grow-1 border-start-0" value="@Model.ProfilePictureUrl" placeholder="https://example.com/photo.jpg" aria-label="profilePictureUrl">
                                <button class="btn btn-outline-secondary border-2" type="button" onclick="document.getElementById('profilePictureFile').click()">
                                    <i class="fas fa-upload"></i>
                                </button>
                            </div>
                            <input type="file" name="profilePictureFile" id="profilePictureFile" hidden accept="image/*" / aria-label="profilePictureFile">
                            <div class="form-text text-xs mt-2 text-muted">Enter a URL or click the upload icon to choose a file for Cloudinary.</div>
                        </div>
                        <button type="submit" class="ux-btn-primary rounded-pill px-5">
                            <i class="fas fa-save me-2"></i>Save Profile
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Accessibility Settings -->
        <div class="col-lg-6">
            <div class="card border-0 shadow-sm rounded-lg overflow-hidden h-100 ux-card">
                <div class="card-header bg-white py-3 border-bottom d-flex align-items-center">
                    <h6 class="fw-bold mb-0 text-dark">Accessibility Preferences</h6>
                </div>
                <div class="card-body p-4">
                    <form asp-action="UpdateSettings" method="post">
                        <div class="mb-4">
                            <label for="fontSizeSelect" class="modern-form-label">Font Size</label>
                            <select id="fontSizeSelect" name="fontSize" class="modern-form-control w-100" aria-label="fontSize">
                                <option value="default" selected="@(Model.FontSize == "default")">Default (Regular)</option>
                                <option value="large" selected="@(Model.FontSize == "large")">Large (120%)</option>
                                <option value="extra-large" selected="@(Model.FontSize == "extra-large")">Extra Large (140%)</option>
                            </select>
                            <div class="form-text text-xs mt-2 text-muted">Adjust the text size for better readability across the application.</div>
                        </div>

                        <div class="mb-4">
                            <label class="modern-form-label d-block mb-3">Display Enhancements</label>
                            
                            <div class="form-check form-switch mb-3 p-0">
                                <div class="d-flex justify-content-between align-items-center px-2 py-2 rounded transition-all hover-bg-light">
                                    <label class="form-check-label fw-semibold text-dark cursor-pointer" for="highContrastSwitch">
                                        High Contrast Mode
                                    </label>
                                    <input class="form-check-input ms-0" type="checkbox" name="isHighContrast" id="highContrastSwitch" value="true" @(Model.IsHighContrast ? "checked" : "") aria-label="isHighContrast">
                                </div>
                                <div class="text-muted text-xs ms-2">Increases visibility for icons and text.</div>
                            </div>

                            <div class="form-check form-switch mb-0 p-0">
                                <div class="d-flex justify-content-between align-items-center px-2 py-2 rounded transition-all hover-bg-light">
                                    <label class="form-check-label fw-semibold text-dark cursor-pointer" for="reduceMotionSwitch">
                                        Reduced Motion
                                    </label>
                                    <input class="form-check-input ms-0" type="checkbox" name="reduceMotion" id="reduceMotionSwitch" value="true" @(Model.ReduceMotion ? "checked" : "") aria-label="reduceMotion">
                                </div>
                                <div class="text-muted text-xs ms-2">Silences animations and transitions.</div>
                            </div>
                        </div>

                        <button type="submit" class="ux-btn-primary rounded-pill px-5">
                            <i class="fas fa-magic me-2"></i>Apply Changes
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <!-- Password Change Section -->
        <div class="col-lg-12">
            @if (Context.Session.GetString("ForcePasswordChange") == "true")
            {
                <div class="alert alert-warning border-warning shadow-sm mb-4 ux-animate-bounce-in">
                    <div class="d-flex align-items-center">
                        <i class="fas fa-exclamation-triangle fa-2x me-3 text-warning"></i>
                        <div>
                            <h6 class="fw-bold mb-1">Mandatory Password Update Required</h6>
                            <p class="mb-0 small">Security policy requires you to update your password before continuing. This could be due to an expired password or an administrative reset.</p>
                        </div>
                    </div>
                </div>
            }

            <div class="card border-0 shadow-sm rounded-lg overflow-hidden ux-card mt-0">
                <div class="card-header bg-white py-3 border-bottom d-flex justify-content-between align-items-center">
                    <h6 class="fw-bold mb-0 text-dark">Security Settings</h6>
                    <div class="mfa-status-pill">
                        @if (Model.TwoFactorEnabled)
                        {
                            <span class="badge bg-success-soft text-success border border-success-subtle rounded-pill px-3 py-2">
                                <i class="fas fa-check-circle me-1"></i> 2FA Enabled
                            </span>
                        }
                        else
                        {
                            <span class="badge bg-secondary-soft text-secondary border border-secondary-subtle rounded-pill px-3 py-2">
                                <i class="fas fa-times-circle me-1"></i> 2FA Disabled
                            </span>
                        }
                    </div>
                </div>
                <div class="card-body p-4">
                    <div class="row g-4">
                        <!-- MFA Management -->
                        <div class="col-md-12 mb-4">
                            <div class="p-4 bg-light rounded-lg border d-flex align-items-center justify-content-between">
                                <div class="d-flex align-items-center">
                                    <div class="p-3 rounded-circle bg-white text-primary shadow-sm me-3">
                                        <i class="fas fa-mobile-alt fa-lg"></i>
                                    </div>
                                    <div>
                                        <h6 class="fw-bold mb-1">Two-Factor Authentication (MFA)</h6>
                                        <p class="text-muted small mb-0">Add an extra layer of security to your account using an authenticator app.</p>
                                    </div>
                                </div>
                                <div>
                                    @if (!Model.TwoFactorEnabled)
                                    {
                                        <a asp-action="SetupMfa" class="btn btn-primary rounded-pill px-4">
                                            Enable MFA
                                        </a>
                                    }
                                    else
                                    {
                                        <form asp-action="DisableMfa" method="post" onsubmit="return confirm('Are you sure you want to disable 2FA? This will make your account less secure.');">
                                            <button type="submit" class="btn btn-outline-danger rounded-pill px-4">
                                                Disable MFA
                                            </button>
                                        </form>
                                    }
                                </div>
                            </div>
                        </div>

                        <div class="col-12">
                            <hr class="mt-0 mb-4" />
                            <h6 class="fw-bold mb-3">Update Password</h6>
                        </div>

                        <form asp-action="ChangePassword" method="post" class="w-100">
                            <div class="row">
                                <div class="col-md-4">
                                    <label for="currentPassword" class="modern-form-label">Current Password</label>
                                    <input type="password" name="CurrentPassword" id="currentPassword" class="modern-form-control w-100" required aria-label="CurrentPassword">
                                </div>
                                <div class="col-md-4">
                                    <label for="newPassword" class="modern-form-label">New Password</label>
                                    <div class="password-toggle-group position-relative">
                                        <input type="password" name="NewPassword" id="newPassword" class="modern-form-control w-100" required style="padding-right: 45px;" / aria-label="NewPassword">
                                    <partial name="_PasswordRequirements" model='("newPassword", "submitPasswordBtn", Model.Username, true, "")' />
                                </div>
                                <div class="col-md-4">
                                    <label for="confirmPassword" class="modern-form-label">Confirm New Password</label>
                                    <div class="password-toggle-group position-relative">
                                        <input type="password" name="ConfirmPassword" id="confirmPassword" class="modern-form-control w-100" required / aria-label="ConfirmPassword">
                                    </div>
                                </div>
                            </div>
                            <div class="mt-4">
                                <button type="submit" id="submitPasswordBtn" class="ux-btn-primary rounded-pill px-5" style="background: var(--espresso-dark);" disabled>
                                    <i class="fas fa-key me-2"></i>Change Password
                                </button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        function toggleNewConfirmPassword(btn) {
            const newPass = document.getElementById('newPassword');
            const confPass = document.getElementById('confirmPassword');
            const icon = btn.querySelector('i');
            
            if (newPass.type === 'password') {
                newPass.type = 'text';
                confPass.type = 'text';
                icon.classList.remove('fa-eye');
                icon.classList.add('fa-eye-slash');
            } else {
                newPass.type = 'password';
                confPass.type = 'password';
                icon.classList.remove('fa-eye-slash');
                icon.classList.add('fa-eye');
            }
        }
    </script>
}
